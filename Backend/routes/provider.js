// const express = require("express");
// const Provider = require("../controllers/provider");
// const { getProviderById } = require("../controllers/provider");

// var router = express.Router();

// router.get("/:id", getProviderById);
// router.get("/providers/category/:category_id", Provider.providerCategory);
// router.get("/providers/top", Provider.topProviders);
// router.get("/providers/top/:category_id", Provider.topCategoryProviders);

// module.exports = router;
